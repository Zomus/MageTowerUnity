using UnityEngine;
using System.Collections;

public class SpringTrapController : MonoBehaviour {

	bool ready;
	//whether the trap is ready to go

	// Use this for initialization
	void Start () {
		ready = true;
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
